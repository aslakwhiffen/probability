TRUE. Since the set $ \{ |X - \E X| \geq a \} = \{ X - \E X \geq a \} \cup \{ \E X - X \geq a \} $,
we have that $ \{ X - \E X \geq a \} \subset \{ |X - \E X | \geq a\} $ and so by the monotonic
property of probabilities we get that $ \P( \{ X - \E X \geq a \} ) \leq \P( \{ |X- \E X| \geq a\}
)$ and using Markov's inequality we get $ \P( \{ X - \E X \geq a \} ) \leq \mathrm{Var}(X)/a^2$.

